1) c edit.blade  отправляю данные имя сырьё(текст) постом через роут на контроллер где проверяю если такой айди в базе
и помещаю в очередь (через редис ) класс
 translateProductJod.php
 # TranslateProductJob::dispatch($product, $data['source_text']); Laravel берет этот объект, превращает его в JSON-строку и кладет в Redis. На этом роль контроллера закончена.
 где я подкючаю сервис (openAiService который получает (текст - сырьё) + пишем промт - инструкцию) где (происходит обращение к опениа) получив ответ прохожусь по языковым получив текст конкретной статьи и закид в базу
